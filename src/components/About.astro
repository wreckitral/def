<section id="about" class="about-section">
  <div class="container">
    <h2 class="section-title">About Me</h2>

    <div class="terminal-window">
      <div class="terminal-header">
        <div class="terminal-dots">
          <div class="dot red"></div>
          <div class="dot yellow"></div>
          <div class="dot green"></div>
        </div>
        <div class="terminal-title">about-me.sh</div>
      </div>
      <div class="terminal-body">
        <div class="command-line">
          <span class="prompt">$ </span><span class="command">whoami</span>
        </div>
        <div class="output">
          Backend Developer, trying to deep dive in the world of GPU
          programming, Vim & linux daily-user
        </div>

        <div class="command-line">
          <span class="prompt">$ </span><span class="command"
            >cat experience.txt</span
          >
        </div>
        <div class="output">
          I've been getting serious into the world of computer since early 2021,
          did my first internship as software engineer intern, i build a
          forecasting system for product sales using Python and XGBoost. and
          then did a contract job building official website backend for Research
          and Community Service Institute at Sriwijaya University, in my college
          days, i did competitive programming competition, The speaker of AI
          Infrastructure and Operation Training at my faculty, tutored for new
          student on The Fundamental of Programming, led a class disassembling
          and assembling a PC for my juniors, awarded for being an outstanding
          student by PT Pegadaian.

          <p
            style="color: var(--command-help); font-style: italic; margin: 0.5rem;"
          >
            check out my <a href="#contact">Linkedin</a> for Photos.
          </p>
        </div>

        <div class="command-line">
          <span class="prompt">$ </span><span class="command">ls skills/</span>
        </div>
        <div class="output file-list">
          backend/ gpu/ mlops/ distributed-system/ linux/ devops/ databases/
        </div>

        <div class="command-line">
          <span class="prompt">$ </span><span class="command"
            >echo $PHILOSOPHY</span
          >
        </div>
        <div class="output success">
          "As long as my brain functions, learning is my favorite runtime
          process"
        </div>

        <div class="terminal-cursor"></div>
      </div>
    </div>

    <div class="about-cards">
      <div class="about-card">
        <h3>Building</h3>
        <p>
          Right now, im focusing on my <a
            href="https://github.com/wreckitral/skripsi-lab"
            target="_blank"
            rel="noopener noreferrer">thesis</a
          >
          which is low-level optimization on matrix multiplication, the idea is determining
          whether its worth the performance or not to handwrite the PTX.
        </p>
        <p>
          Developing a <a
            href="https://github.com/wreckitral/inference-server"
            target="_blank"
            rel="noopener noreferrer"
          >
            Scalable LLM Inference server</a
          > in Go & C++, to showcase my skills on backend, cuda, and mlops.
        </p>
      </div>

      <div class="about-card">
        <h3>Learning</h3>
        <p>
          Deep diving into the GPU programming, HPC, Distributed System, System
          Design.
        </p>
      </div>

      <div class="about-card">
        <h3>Other</h3>
        <p>
          Beyond coding im building my split keyboard, its a low profile <a
            href="https://github.com/foostan/crkbd"
            target="_blank"
            rel="noopener noreferrer">corne</a
          >
          v4 keyboard.
        </p>

        <p>
          Im reading this github on brief of system design recommended by my
          fellow dev friend,
          <a
            href="https://github.com/ByteByteGoHq/system-design-101"
            target="_blank"
            rel="noopener noreferrer"
          >
            ByteByteGo</a
          >
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding: 4rem 0;
    border-bottom: 1px solid var(--border-color);
  }

  .section-title {
    font-size: 2rem;
    font-weight: 600;
    color: var(--accent-blue);
    margin-bottom: 2rem;
    position: relative;
    line-height: 1.2;
  }

  .section-title::before {
    content: "# ";
    color: var(--text-secondary);
  }

  .terminal-window {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    margin: 2rem 0;
  }

  .terminal-header {
    background: var(--bg-tertiary);
    padding: 0.8rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    border-bottom: 1px solid var(--border-color);
  }

  .terminal-dots {
    display: flex;
    gap: 0.5rem;
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .dot.red {
    background: #ff5f56;
  }
  .dot.yellow {
    background: #ffbd2e;
  }
  .dot.green {
    background: #27ca3f;
  }

  .terminal-title {
    margin-left: 1rem;
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .terminal-body {
    padding: 2rem;
    font-size: 0.95rem;
    line-height: 1.8;
  }

  .command-line {
    margin: 1rem 0 0.2rem 0;
  }

  .prompt {
    color: var(--success);
  }

  .command {
    color: var(--warning);
  }

  .output {
    color: var(--text-primary);
    margin: 0.5rem 0 1rem 0;
    padding-left: 1rem;
  }

  .output.file-list {
    color: var(--accent-blue);
  }

  .output.success {
    color: var(--success);
    font-weight: 500;
  }

  .terminal-cursor {
    display: inline-block;
    width: 10px;
    height: 20px;
    background: var(--accent-blue);
    animation: blink 1s infinite;
    margin-left: 2px;
    margin-top: 1rem;
  }

  @keyframes blink {
    0%,
    50% {
      opacity: 1;
    }
    51%,
    100% {
      opacity: 0;
    }
  }

  .about-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .about-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    padding: 2rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .about-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(
      90deg,
      var(--accent-blue) 0%,
      var(--accent-blue-hover) 100%
    );
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .about-card:hover::before {
    transform: scaleX(1);
  }

  .about-card:hover {
    transform: translateY(-4px);
    border-color: var(--accent-blue-dim);
    box-shadow: 0 8px 32px rgba(88, 166, 255, 0.1);
  }

  .about-card h3 {
    font-size: 1.3rem;
    font-weight: 600;
    color: var(--accent-blue);
    margin-bottom: 1rem;
  }

  .about-card p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .about-section {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 1.5rem;
    }

    .about-cards {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .terminal-body {
      padding: 1.5rem;
      font-size: 0.9rem;
    }

    .about-card {
      padding: 1.5rem;
    }

    .about-card h3 {
      font-size: 1.2rem;
    }
  }

  @media (max-width: 480px) {
    .about-section {
      padding: 2rem 0;
    }

    .section-title {
      font-size: 1.3rem;
      margin-bottom: 1.5rem;
    }

    .about-cards {
      gap: 1.25rem;
    }

    .terminal-body {
      padding: 1.25rem;
      font-size: 0.85rem;
    }

    .about-card {
      padding: 1.25rem;
    }

    .about-card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.75rem;
    }

    .command-line {
      margin: 0.75rem 0 0.1rem 0;
    }

    .output {
      margin: 0.25rem 0 0.75rem 0;
      padding-left: 0.75rem;
    }
  }
</style>
