<section id="contact" class="contacts-section">
  <div class="container">
    <h2 class="section-title">Get In Touch</h2>

    <div class="terminal-window">
      <div class="terminal-header">
        <div class="terminal-dots">
          <div class="dot red"></div>
          <div class="dot yellow"></div>
          <div class="dot green"></div>
        </div>
        <div class="terminal-title">contact-info.sh</div>
      </div>
      <div class="terminal-body">
        <div class="command-line">
          <span class="prompt">$ </span><span class="command"
            >curl -X GET /api/contact/info</span
          >
        </div>
        <div class="output info">Status: 200 OK - Ready to connect!</div>

        <div class="command-line">
          <span class="prompt">$ </span><span class="command"
            >echo "Let's collaborate!!!"</span
          >
        </div>
        <div class="output success">Reach me anytime XD</div>

        <div class="terminal-cursor"></div>
      </div>
    </div>

    <div class="contact-content">
      <div class="contact-info">
        <div class="info-card">
          <div class="card-header">
            <span class="card-icon">📧</span>
            <h3>Email</h3>
          </div>
          <p class="contact-detail">defhanayasofhiea@gmail.com</p>
          <a href="mailto:defhanayasofhiea@gmail.com" class="contact-link">
            <span class="link-icon">→</span>
            Send Email
          </a>
        </div>

        <div class="info-card">
          <div class="card-header">
            <span class="card-icon">💼</span>
            <h3>LinkedIn</h3>
          </div>
          <p class="contact-detail">Connect professionally</p>
          <a
            href="https://linkedin.com/in/defhanaya"
            class="contact-link"
            target="_blank"
            rel="noopener"
          >
            <span class="link-icon">→</span>
            View Profile
          </a>
        </div>

        <div class="info-card">
          <div class="card-header">
            <span class="card-icon">⚡</span>
            <h3>GitHub</h3>
          </div>
          <p class="contact-detail">Check out my code</p>
          <a
            href="https://github.com/wreckitral"
            class="contact-link"
            target="_blank"
            rel="noopener"
          >
            <span class="link-icon">→</span>
            View Repos
          </a>
        </div>

        <div class="info-card">
          <div class="card-header">
            <span class="card-icon">🐦</span>
            <h3>Twitter</h3>
          </div>
          <p class="contact-detail">This is where i yap</p>
          <a
            href="https://twitter.com/_scramblecode"
            class="contact-link"
            target="_blank"
            rel="noopener"
          >
            <span class="link-icon">→</span>
            Follow Me
          </a>
        </div>
      </div>

      <div class="contact-form-container">
        <div class="form-terminal">
          <div class="terminal-header">
            <div class="terminal-dots">
              <div class="dot red"></div>
              <div class="dot yellow"></div>
              <div class="dot green"></div>
            </div>
            <div class="terminal-title">
              send-message.sh <strong>(on development)</strong>
            </div>
          </div>
          <div class="terminal-body">
            <div class="form-intro">
              <div class="command-line">
                <span class="prompt">$ </span><span class="command"
                  >./send-message --interactive</span
                >
              </div>
              <div class="output">
                Initializing secure message transmission...
              </div>
            </div>

            <form class="contact-form" action="#" method="POST">
              <div class="form-group">
                <label for="name" class="form-label">
                  <span class="prompt">name@client:~$ </span>Enter your name:
                </label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  class="form-input"
                  placeholder="Udin Keren"
                  required
                />
              </div>

              <div class="form-group">
                <label for="email" class="form-label">
                  <span class="prompt">email@client:~$ </span>Enter your email:
                </label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  class="form-input"
                  placeholder="udin@example.com"
                  required
                />
              </div>

              <div class="form-group">
                <label for="subject" class="form-label">
                  <span class="prompt">subject@client:~$ </span>Message subject:
                </label>
                <input
                  type="text"
                  id="subject"
                  name="subject"
                  class="form-input"
                  placeholder="Project Collaboration"
                  required
                />
              </div>

              <div class="form-group">
                <label for="message" class="form-label">
                  <span class="prompt">message@client:~$ </span>Your message:
                </label>
                <textarea
                  id="message"
                  name="message"
                  class="form-textarea"
                  placeholder="Hi! I'd love to discuss a potential project..."
                  rows="6"
                  required></textarea>
              </div>

              <div class="form-actions">
                <button type="submit" class="submit-button">
                  <span class="button-icon">→</span>
                  <span class="button-text">Execute Send</span>
                </button>
                <div class="form-status">
                  <span class="status-text">Ready to transmit</span>
                </div>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <div class="availability-section">
      <div class="availability-card">
        <div class="card-header">
          <span class="status-indicator active"></span>
          <h3>Current Status</h3>
        </div>
        <p class="availability-text">
          <span class="success">Available for new projects</span> - I'm currently
          open to work and collaboration opportunities.
        </p>
        <div class="response-time">
          <span class="time-label">Typical response time:</span>
          <span class="time-value">2 hours</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contacts-section {
    padding: 4rem 0;
  }

  .section-title {
    font-size: 2rem;
    font-weight: 600;
    color: var(--accent-blue);
    margin-bottom: 2rem;
    position: relative;
    line-height: 1.2;
  }

  .section-title::before {
    content: "# ";
    color: var(--text-secondary);
  }

  .terminal-window {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    margin: 2rem 0;
  }

  .terminal-header {
    background: var(--bg-tertiary);
    padding: 0.8rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    border-bottom: 1px solid var(--border-color);
  }

  .terminal-dots {
    display: flex;
    gap: 0.5rem;
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .dot.red {
    background: #ff5f56;
  }
  .dot.yellow {
    background: #ffbd2e;
  }
  .dot.green {
    background: #27ca3f;
  }

  .terminal-title {
    margin-left: 1rem;
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .terminal-body {
    padding: 2rem;
    font-size: 0.95rem;
    line-height: 1.8;
  }

  .command-line {
    margin: 1rem 0 0.2rem 0;
  }

  .prompt {
    color: var(--success);
  }

  .command {
    color: var(--warning);
  }

  .output {
    color: var(--text-primary);
    margin: 0.5rem 0 1rem 0;
    padding-left: 1rem;
  }

  .output.info {
    color: var(--accent-blue);
    font-weight: 500;
  }

  .output.success {
    color: var(--success);
  }

  .terminal-cursor {
    display: inline-block;
    width: 10px;
    height: 20px;
    background: var(--accent-blue);
    animation: blink 1s infinite;
    margin-left: 2px;
    margin-top: 1rem;
  }

  @keyframes blink {
    0%,
    50% {
      opacity: 1;
    }
    51%,
    100% {
      opacity: 0;
    }
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    margin: 3rem 0;
  }

  .contact-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
  }

  .info-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    padding: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .info-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(
      90deg,
      var(--accent-blue) 0%,
      var(--accent-blue-hover) 100%
    );
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .info-card:hover::before {
    transform: scaleX(1);
  }

  .info-card:hover {
    transform: translateY(-2px);
    border-color: var(--accent-blue-dim);
    box-shadow: 0 4px 16px rgba(88, 166, 255, 0.1);
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    margin-bottom: 1rem;
  }

  .card-icon {
    font-size: 1.2rem;
  }

  .card-header h3 {
    color: var(--accent-blue);
    font-size: 1.1rem;
    font-weight: 600;
  }

  .contact-detail {
    color: var(--text-secondary);
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  .contact-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--accent-blue);
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.2s ease;
  }

  .contact-link:hover {
    color: var(--accent-blue-hover);
  }

  .link-icon {
    font-size: 0.8rem;
  }

  .form-terminal {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }

  .form-intro {
    margin-bottom: 2rem;
  }

  .contact-form {
    margin-top: 2rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-label {
    display: block;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
  }

  .form-input,
  .form-textarea {
    width: 100%;
    background: var(--bg-primary);
    border: 1px solid var(--border-color);
    border-radius: 4px;
    padding: 0.8rem;
    color: var(--text-primary);
    font-family: inherit;
    font-size: 0.9rem;
    transition: border-color 0.2s ease;
  }

  .form-input:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--accent-blue);
    box-shadow: 0 0 0 2px rgba(88, 166, 255, 0.1);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: var(--text-secondary);
  }

  .form-textarea {
    resize: vertical;
    min-height: 120px;
  }

  .form-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 2rem;
  }

  .submit-button {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--accent-blue);
    color: white;
    border: none;
    border-radius: 4px;
    padding: 0.8rem 1.5rem;
    font-family: inherit;
    font-size: 0.9rem;
    font-weight: 500;
    cursor: pointer;
    transition: background 0.2s ease;
  }

  .submit-button:hover {
    background: var(--accent-blue-hover);
  }

  .button-icon {
    font-size: 0.8rem;
  }

  .form-status {
    color: var(--text-secondary);
    font-size: 0.8rem;
  }

  .availability-section {
    margin-top: 3rem;
  }

  .availability-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    padding: 2rem;
    text-align: center;
  }

  .availability-card .card-header {
    justify-content: center;
    margin-bottom: 1.5rem;
  }

  .status-indicator {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    display: inline-block;
  }

  .status-indicator.active {
    background: var(--success);
    box-shadow: 0 0 10px rgba(35, 134, 54, 0.3);
  }

  .availability-text {
    color: var(--text-secondary);
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  .availability-text .success {
    color: var(--success);
    font-weight: 500;
  }

  .response-time {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
  }

  .time-label {
    color: var(--text-secondary);
    font-size: 0.9rem;
  }

  .time-value {
    color: var(--accent-blue);
    font-weight: 500;
  }

  @media (max-width: 1024px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .contact-info {
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }
  }

  @media (max-width: 768px) {
    .contact-content {
      gap: 2rem;
    }

    .contact-info {
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    }
  }

  @media (max-width: 768px) {
    .contacts-section {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 1.5rem;
    }

    .terminal-body {
      padding: 1.5rem;
      font-size: 0.9rem;
    }

    .contact-content {
      gap: 2rem;
    }

    .contact-info {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .info-card {
      padding: 1.2rem;
    }

    .form-actions {
      flex-direction: column;
      gap: 1rem;
      align-items: stretch;
    }

    .submit-button {
      justify-content: center;
      padding: 0.75rem 1.25rem;
    }

    .form-group {
      margin-bottom: 1.25rem;
    }

    .form-input,
    .form-textarea {
      font-size: 0.85rem;
      padding: 0.75rem;
    }
  }

  @media (max-width: 480px) {
    .contacts-section {
      padding: 2rem 0;
    }

    .section-title {
      font-size: 1.3rem;
      margin-bottom: 1.5rem;
    }

    .terminal-body {
      padding: 1.25rem;
      font-size: 0.85rem;
    }

    .contact-content {
      gap: 1.5rem;
    }

    .contact-info {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .info-card {
      padding: 1rem;
    }

    .card-header {
      flex-direction: column;
      text-align: center;
      gap: 0.5rem;
    }

    .card-header h3 {
      font-size: 1rem;
    }

    .contact-detail {
      font-size: 0.85rem;
      margin-bottom: 0.75rem;
    }

    .contact-link {
      font-size: 0.85rem;
    }

    .availability-card {
      padding: 1.5rem;
    }

    .availability-card .card-header h3 {
      font-size: 1rem;
    }

    .availability-text {
      font-size: 0.9rem;
    }
  }
</style>
